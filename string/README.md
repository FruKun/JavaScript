# Поиск подстроки в строке
## постановка задачи
Дана строка S длины n и строка T длины m. В дальнейшем, не оговаривая каждый раз
отдельно, будем предполагать, что n ≥ m. Будем нумеровать символы строк начиная с 1. Обозначим через S[i .. j], 1 ≤ i ≤ j ≤ n, подстроку строки S, начинающуюся в i-й
позиции и заканчивающуюся в j-й позиции. Требуется найти все вхождения строки T
в строку S, т.е. указать все позиции строки S, начиная с которых читается строка T.
Множество вхождений, которое надо построить, формально описывается так M = {i|1 ≤
i ≤ n − m + 1, S[i .. i + m] = T }.
Например, пусть S = abcabdecab, T = cab, тогда M = {3; 8}. Или такой пример, пусть S = abababacaba, T = aba, тогда M = {1; 3; 5; 9}. Таким образом мы допускаем "перекрытие" - ситуацию, когда расстояние между двумя соседними вхождениями меньше длины
строки T.  

Подробнее: "Солодушкин Святослав Игоревич, Учебное пособие по дисциплине скрипты" глава 4
## запуск
### Метод решения - алгоритм грубой силы
```sh
node bruteforce.js
```
### Метод решения - использование хэшей
```sh
node search.js
```